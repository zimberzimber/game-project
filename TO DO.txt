
        TO DO:
- Add a UI scale factor
- Add a method which generates button or pane sprites and adds them to the sprite collection

        Done:
X Sprites can now display part of the image (cutoff). (Like a progress bar)
X Moved animation loop request to the end of the update function in Game
X Added a proper keymap class
X Keys are now passed as enums
X Added key and mouse click simulation to input controls
X Added controller supports