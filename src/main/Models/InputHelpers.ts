export class KeyDefs {
    up: number;
    down: number;
    left: number;
    right: number;
    pause: number;
    interact: number;
    cancel: number;
}

export enum KeyNames { up, down, left, right, pause, cancel, interact }